type t = {
  friendly_name : string;
  phone_number : string;
  lata : string;
  rate_center : string;
  latitude : string;  (* ought to be float, but is sent in quotes *)
  longitude : string;
  region : string;
  postal_code : string;
  iso_country : string;
}

type response = {
  uri : string;
  available_phone_numbers : t list
}
